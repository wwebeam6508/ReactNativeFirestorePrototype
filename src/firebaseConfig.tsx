
import {APP_API_KEY, APP_AUTH_DOMAIN, APP_DATA_BASE_URL, 
    APP_PROJECT_ID, APP_STORAGE_BUCKET, APP_MESSAGING_SENDER_ID, 
    APP_APP_ID, APP_MEASUREMENT_ID} from '@env'
export const firebaseConfig = {
    apiKey: APP_API_KEY,
    authDomain: APP_AUTH_DOMAIN,
    databaseURL: APP_DATA_BASE_URL,
    projectId: APP_PROJECT_ID,
    storageBucket: APP_STORAGE_BUCKET,
    messagingSenderId: APP_MESSAGING_SENDER_ID,
    appId: APP_APP_ID,
    measurementId: APP_MEASUREMENT_ID
  }